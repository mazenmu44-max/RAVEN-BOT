const { MessageEmbed } = require('discord.js')
module.exports = {
    name : 'tp',
    run : async (client, message, args) => {
        const embed = new MessageEmbed()
        .addField('', '[**Lock**](https://discord.gg/Ct52UDvuR6) the voice channel')
    }
}